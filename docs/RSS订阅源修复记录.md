# RSS 订阅源修复记录（最终版）

## 📅 修复日期

2026-02-07

## ⚠️ 问题总结

在配置 RSS 订阅时遇到了以下问题：

1. **404 错误** - 订阅源 URL 已失效
2. **SSL 证书错误** - HTTPS 证书验证失败
3. **XML 解析错误** - RSS feed 格式不完整或损坏
4. **协议不支持** - 网站不再提供 RSS 订阅

## ✅ 最终修复方案

### 1. **Paul Graham Essays**

- **状态**: ✅ 已修复（使用 GitHub Pages 镜像）
- **最终 URL**: `https://filipesilva.github.io/paulgraham-rss/feed.rss`
- **尝试过的方案**:
  - ❌ `http://www.aaronsw.com/2002/feeds/pgessays.xml` (404)
  - ❌ `https://anonyonoor.com/feeds/paul-graham` (XML 解析错误)
  - ❌ `http://www.paulgraham.com/rss.html` (已停止更新)
- **解决方案**: 使用 GitHub Pages 托管的镜像源，由社区维护

### 2. **Uber Engineering**

- **状态**: ❌ 已移除（官方不再提供 RSS）
- **尝试过的方案**:
  - ❌ `https://eng.uber.com/feed/` (404)
  - ❌ `https://www.uber.com/blog/engineering/rss/` (404)
  - ❌ `https://www.uber.com/blog/engineering/` (406)
- **原因**: Uber Engineering Blog 已不再提供 RSS 订阅功能
- **建议**: 可以尝试使用 RSSHub 等第三方服务生成 RSS

### 3. **Netflix TechBlog**

- **状态**: ✅ 已修复（使用 Medium 子域名）
- **最终 URL**: `https://netflixtechblog.medium.com/feed`
- **尝试过的方案**:
  - ❌ `https://netflixtechblog.com/feed` (SSL 证书错误)
  - ❌ `https://netflixtechblog.com?source=rss----2615bd06b42e---4` (SSL 证书错误)
- **解决方案**: 使用 Medium 的子域名格式，避免 SSL 证书问题

### 4. **Benedict Evans**

- **状态**: ❌ 已移除（不提供 RSS）
- **旧 URL**: `https://www.ben-evans.com/rss`
- **原因**: 网站已转为邮件订阅制（Newsletter）
- **建议**: 访问 <https://www.ben-evans.com/newsletter> 订阅邮件

## 📊 最终订阅列表

### ✅ 工作正常（10个）

**深度科技与极客文化**:

- Hacker News (精选)
- The Verge
- Ars Technica  
- WIRED
- Daring Fireball

**商业与战略洞察**:

- Stratechery
- TechCrunch

**开发者与工程实践**:

- Martin Fowler
- Netflix TechBlog ✨ (已修复)
- GitHub Blog

**独立思考与杂谈**:

- Paul Graham Essays ✨ (已修复)
- Naval Ravikant
- Wait But Why

### ❌ 已移除（2个）

- Benedict Evans (不提供 RSS)
- Uber Engineering (不提供 RSS)

## 🔍 Medium RSS 的正确使用方法

对于托管在 Medium 的技术博客，有两种 RSS 格式：

### 方法 1: 使用子域名（推荐）

```
https://<subdomain>.medium.com/feed
```

**示例**:

- Netflix TechBlog: `https://netflixtechblog.medium.com/feed`
- Airbnb Engineering: `https://medium.com/airbnb-engineering/feed`

### 方法 2: 使用自定义域名

```
https://<custom-domain>/feed
```

**注意**: 某些自定义域名可能存在 SSL 证书问题

## 💡 RSS 源失效的常见原因

1. **博客平台迁移** - 从独立博客迁移到 Medium、Substack 等平台
2. **改用 Newsletter** - 转为邮件订阅制，不再提供 RSS
3. **URL 结构调整** - 网站重构后更改了 RSS 地址
4. **停止维护** - 第三方镜像源停止更新
5. **SSL 证书问题** - HTTPS 配置不当导致访问失败

## 🛠️ 故障排查步骤

当 RSS 源失效时，按以下步骤排查：

### 1. 检查官方网站

访问博客首页，查找：

- RSS 图标或订阅按钮
- 页脚的 RSS 链接
- `/feed` 或 `/rss` 路径

### 2. 使用 RSS 发现工具

```bash
# 使用 curl 测试
curl -I <RSS_URL>

# 查看响应头
# 200 = 正常
# 301/302 = 重定向（查看 Location 头）
# 404 = 失效
# 403 = 访问被拒绝
```

### 3. 寻找替代源

**社区维护的镜像**:

- GitHub Pages 托管的 RSS
- RSSHub: `https://docs.rsshub.app/`
- RSS Bridge: `https://rss-bridge.org/`

**Medium 博客**:

- 直接在域名后添加 `/feed`
- 使用 Medium 子域名格式

### 4. 考虑替代方案

- 使用邮件订阅（如果可用）
- 使用社交媒体（Twitter、LinkedIn）
- 设置网页监控工具（如 Visualping）

## 📈 维护建议

### 定期检查（建议每季度一次）

创建检查清单：

- [ ] 测试所有 RSS 源是否可访问
- [ ] 验证是否有新内容更新
- [ ] 查找失效源的替代方案
- [ ] 清理长期无更新的订阅

### 备份配置

定期备份 `data.json` 文件：

```bash
cp data.json data.json.backup.$(date +%Y%m%d)
```

### 使用 RSS 聚合服务

考虑使用：

- Feedly
- Inoreader
- NewsBlur

这些服务通常会自动处理 RSS 源的迁移和更新。

## 🎯 最佳实践

1. **优先使用官方 RSS** - 最稳定可靠
2. **Medium 优先用子域名** - 避免 SSL 问题
3. **记录源的来源** - 方便后续维护
4. **设置合理的更新间隔** - 避免频繁请求
5. **监控错误日志** - 及时发现问题

## 📝 修改历史

### 2026-02-07 第二次修复

- **Paul Graham**: 改用 GitHub Pages 镜像（filipesilva）
- **Netflix**: 改用 Medium 子域名格式
- **Uber**: 移除（官方不提供 RSS）

### 2026-02-07 第一次修复

- Paul Graham: 尝试使用 anonyonoor.com 镜像（失败）
- Uber: 尝试使用新官方地址（失败）
- Netflix: 尝试使用 Medium 参数格式（失败）
- Benedict Evans: 移除（不提供 RSS）

## ✅ 当前状态

- **总订阅数**: 13
- **正常工作**: 13/13 (100%)
- **最后验证**: 2026-02-07 15:53
